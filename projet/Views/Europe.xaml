<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:projet"
             x:Class="projet.Views.Page1"
             Title="Europe"
             Icon="icon.png">

    <ListView 
        HorizontalOptions="FillAndExpand"
        VerticalOptions="FillAndExpand"
        ItemsSource="{Binding Countries}"
        SelectedItem="{Binding CountrySelected, Mode=TwoWay}"
        HasUnevenRows="True">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <StackLayout>


                        <Frame CornerRadius="50" BackgroundColor="Black" WidthRequest="50" HeightRequest="50" HorizontalOptions="Start" VerticalOptions="Center" IsClippedToBounds="True" Margin="20" Padding="0">
                            <Image Source="{Binding Flags.Png.AbsoluteUri}" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand"></Image>
                        </Frame>
                        <Label Text="{Binding Name.Common}" HorizontalOptions="Center"/>
                        <Image IsVisible="{Binding IsFavourite}" Source="favourite.png" HeightRequest="16" HorizontalOptions="End" Margin="20"/>
                    </StackLayout>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>

    </ListView>


</ContentPage>